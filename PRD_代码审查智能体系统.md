# PRD: 代码审查智能体系统 (Code Review Agent System)

> **产品名**: CodeBuddy Review（代号）  
> **版本**: v2.0（修订：OpenEvolve 角色从"进化 Prompt"修正为"进化代码本身"）  
> **PRD 状态**: Draft  
> **作者**: 产品通 (ProductManagementExpert)  
> **日期**: 2026-06-11（修订 2026-06-14）  
> **上游文档**: [代码审查智能体系统_架构设计与功能方案](./代码审查智能体系统_架构设计与功能方案.md)

---

## Problem Statement

我们的研发团队面临以下三个核心问题：

1. **代码质量参差不齐** — 团队缺少统一的"好代码"标准，审查结果高度依赖审查员的个人判断，标准今天严明天松，不同人的尺度相差巨大。
2. **可读性差** — 命名不规范、缺少有效注释、代码结构混乱等问题没有系统化检查，人工审查受限于疲劳和时间压力，"能跑就行"心态蔓延。
3. **代码复杂度偏高** — 缺少圈复杂度/认知复杂度的自动化阈值和阻断机制，高复杂度代码只有在引发线上事故后才被重视，审查员很少逐函数计算复杂度指标。

**根因**：不是团队缺少审查流程，而是现有的人工审查机制**无法系统化、不可度量、不可持续**。

**业务影响**：代码腐化导致线上 bug 率上升、新人上手成本增加、迭代速度持续下降。

---

## Solution

构建一套 **"审查 Agent 发现问题 + OpenEvolve 进化代码自动修复"** 的代码审查智能体系统。

**核心流程**：

```
开发者提交 PR
     │
     ▼
Stage 0: 静态门控 — 复杂度>15？SQL拼接？ → 直接阻断，返回开发者
     │ (通过)
     ▼
审查 Agent — 分析 diff，将问题分为两类：
  ├── 🧠 需人工判断（安全漏洞怎么修、架构问题）→ 传统方式给修复建议
  └── 🤖 可自动优化（复杂度过高、性能差、可读性差）→ 交给 OpenEvolve
           │
           ▼
      OpenEvolve 进化引擎
      ├── 输入：高复杂度函数 + 功能正确性测试
      ├── 进化：MAP-Elites 网格探索，LLM Ensemble 生成候选代码
      ├── 评估：功能测试（硬门控）+ 圈复杂度 + 性能 benchmark + 可读性
      └── 输出：功能相同、复杂度更低、性能更好的进化版本
           │
           ▼
      审查报告
      ├── 🔴 阻断项：含 OpenEvolve 进化后的代码作为修复建议
      ├── 🟡 建议项：同上
      └── 📊 进化前后对比（圈复杂度 23→8，性能 +15%）
           │
           ▼
      人工 review → 采纳/驳回/修改 → 反馈回 OpenEvolve
```

**五大审查 Agent** 各司其职，分别覆盖复杂度、可读性、安全、性能和风格五个维度。**OpenEvolve 进化引擎** 是整个系统的差异化核心——它不仅告诉你"这段代码有问题"，更直接给你一个**功能等价但质量更好的版本**。

**OpenEvolve 不会被用来进化 Prompt（那是 Hermes Agent 的范畴），而是用来进化被审查的代码本身。** 审查 Agent 的 Prompt 仍然需要人工维护和调优，但修复建议的生成由 OpenEvolve 的数据驱动的进化搜索来保证质量。

---

## User Stories

### 开发者（提交 PR 的人）

1. 作为一名开发者，我希望在我提交 PR 后的 2 分钟内就看到复杂度、安全等硬性检查结果，以便我知道哪些问题是阻断项、必须立即修复。
2. 作为一名开发者，我希望审查报告中的每个阻断项都附带**具体的修复代码示例**，以便我不需要自己猜测怎么改。
3. 作为一名开发者，我希望能**一键应用补丁**修复 AI 建议的低风险问题（如变量命名、提取常量），以便我花最少时间处理审查反馈。
4. 作为一名开发者，我希望能看到我的代码健康度评分以及和上次 PR 的趋势对比，以便我知道我的代码质量是在改善还是恶化。
5. 作为一名开发者，当我认为 AI 的阻断不合理时，我希望能用 `@exception` 注解声明例外理由，以便不阻塞我的正常工作流。
6. 作为一名开发者，当所有阻断项都解决后，我希望能明确看到 PR 满足合并条件，以便我知道可以通知审查员做 final approval。

### 人工审查员 (Reviewer)

7. 作为一名代码审查员，我希望能看到一份结构化的统一审查报告，而不是零散的 lint 警告，以便我能快速做出判断。
8. 作为一名代码审查员，我希望 AI 已经帮我过滤掉所有风格格式类问题（自动修复），以便我把精力聚焦在架构设计、业务逻辑正确性上。
9. 作为一名代码审查员，当我采纳/驳回/修改 AI 的建议时，我希望系统能记住我的决定和原因，以便后续类似问题的建议更加精准。
10. 作为一名代码审查员，当多个 Agent 的审查意见相互矛盾时，我希望 Arbiter 能给出冲突分析和优先级仲裁，以便我不需要在 Agent 之间自己做判断。
11. 作为一名代码审查员，我希望看到每个审查意见标注了来源 Agent（如 Security Reviewer / Complexity Reviewer）和严重程度标记，以便我快速区分哪些是必须关注的。

### 技术负责人 / Tech Lead

12. 作为一名技术负责人，我希望能看到团队的代码质量趋势看板（平均圈复杂度、阻断项数量、AI 建议采纳率），以便我了解团队的代码质量是在改善还是恶化。
13. 作为一名技术负责人，我希望能审查 OpenEvolve 进化产出的代码变更（diff），在关键决策上保留否决权，以便自动优化不偏离团队编码规范。
14. 作为一名技术负责人，当某个 Agent 的误报率持续偏高时，我希望能暂停或回滚该 Agent 的配置，以便不影响团队开发效率。
15. 作为一名技术负责人，我希望新加入的审查规则采用"先警告→软阻断→硬阻断"的渐进策略，以便团队有适应期而不是突然被大量阻断。

### 产品经理 / PM

16. 作为一名产品经理，我希望了解代码质量下降是否正在影响迭代速度和线上故障率，以便我能量化技术债务对业务的损害。
17. 作为一名产品经理，当我需要说服团队投入时间做代码重构时，我希望能从系统中导出代码健康度报告作为决策依据。

---

## Implementation Decisions

### 模块划分

系统划分为以下独立模块，按 Phase 1→4 分阶段交付：

| 模块 | 职责 | 交付阶段 |
|------|------|---------|
| **Static Gate** (静态门控) | 复杂度硬阻断 + 安全硬阻断 + Lint/Format 检查 + 类型检查；不依赖 LLM，纯规则引擎 | Phase 1 |
| **Diff Analyzer** (差异分析器) | 将 git diff 按文件/函数/类拆分，识别变更类型，关联上下游依赖，提取变更上下文 | Phase 2 |
| **Review Agents** (五大审查智能体) | Complexity / Readability / Security / Performance / Style，各 Agent 独立运行，基于 LLM 做语义审查 | Phase 2 |
| **Arbiter** (仲裁智能体) | 冲突消解、优先级仲裁、将问题分为"需人工判断"和"可自动优化"两类；统一报告聚合 | Phase 2 |
| **Report Generator** (报告生成器) | 生成结构化审查报告，含 OpenEvolve 进化前后对比（复杂度变化、性能变化），输出到 PR Comment / Issue / Slack | Phase 2 |
| **Code Evolver** (代码进化引擎) | 接收审查 Agent 标记的"可自动优化"问题，调用 OpenEvolve 对目标代码进行进化搜索。评估器：功能正确性（硬门控）+ 圈复杂度 + 性能 benchmark + 可读性评分。输出功能等价但质量更高的代码版本作为修复建议 | Phase 2 |
| **Knowledge Base** (知识库) | 反模式库、最佳实践库、审查历史、OpenEvolve 进化历史（哪些优化被采纳/被驳回） | Phase 2 |
| **Feedback Collector** (反馈收集器) | 捕获人工审查员的采纳/驳回/修改操作及原因，反馈给 OpenEvolve 的评估器权重调整 | Phase 2 |
| **Review Prompt Manager** (Prompt 管理器) | 管理各 Agent 的 System Prompt，支持 A/B 测试和版本回滚。Prompt 由人工维护和调优 | Phase 2 |

### 架构决策

- **五阶段流水线**: Stage 0 静态门控（< 2 min）→ Stage 1 差异分析（< 1 min）→ Stage 2 多 Agent 并行审查（3-5 min）→ Stage 3 报告生成（< 1 min）→ Stage 4 人工决断 + 反馈闭环
- **审查优先级**: Security > Correctness > Complexity > Performance > Readability > Style。当不同 Agent 意见冲突时，按此优先级仲裁
- **门控策略**: Stage 0 中复杂度和安全为 🔴 硬阻断，Lint/Style 为 🟡 自动修复或警告；Stage 2 中各 Agent 产出 🔴/🟡/💭 三级问题
- **OpenEvolve 定位**: 进化被审查的代码本身（不是进化 Prompt）。审查 Agent 发现"可自动优化"问题 → OpenEvolve 进化搜索 → 输出功能等价但质量更好的版本作为修复建议。功能正确性测试是硬门控——不通过直接淘汰。
- **优化策略**: 复杂度优化（拆函数、消嵌套）和性能优化（算法替换、缓存）自动进行；安全修复和架构重构由人工审查员决策。
- **隐私安全**: 审查中的代码片段仅发送给 LLM API 用于生成审查意见，不持久化到外部存储。敏感代码仓库使用本地模型或私有化部署
- **渐进上线**: 每个新审查规则采用 "WARN → SOFT_BLOCK → HARD_BLOCK" 三步渐进，每步至少 1 周，给团队适应时间

### 外部依赖

- **LLM API**: Gemini 2.5 Flash（快速审查）/ Gemini 2.5 Pro（安全等关键审查）/ 可选本地 Ollama 部署
- **OpenEvolve**: 开源进化优化框架，用于对目标代码进行进化搜索（发现更好的算法、更低的复杂度、更高的性能）
- **CI/CD 集成**: GitHub Actions / GitLab CI pipeline 中触发，PR comment 形式输出
- **代码质量工具**: ruff (Python) / eslint (JS/TS) — 用于 Stage 0 的 Lint/Format 检查

---

## Testing Decisions

### 测试原则

- **只测试外部行为，不测试实现细节** — 测试"给定一个 diff，Agent 是否能正确标记出问题"，而不是测试 Prompt 的具体文本
- **回归测试必须覆盖** — 对于已知的反模式案例集，每次 Prompt 或规则变更后自动跑回归，确保检出率不下降
- **A/B 测试验证进化效果** — OpenEvolve 进化出的代码版本 vs 人工编写的修复，对比采纳率和代码质量指标
- **功能正确性是硬门控** — 所有 OpenEvolve 产物必须通过原始代码的功能测试才能被采纳

### 测试层级

| 层级 | 内容 | 验证方式 |
|------|------|---------|
| **单元测试** | Static Gate 规则正确性（复杂度计算、安全模式匹配） | pytest 自动化 |
| **集成测试** | 单个 Agent 对预置代码样本的审查输出是否包含预期问题 | 预置样本集 + LLM-as-Judge |
| **回归测试** | 历史已知缺陷是否仍能被检测到（反模式库作为测试集） | CI 自动跑 |
| **端到端测试** | 完整流水线：PR 创建 → 进入 Stage 0→4 → 得到审查报告 | 测试仓库 + 模拟 PR |
| **A/B 测试** | OpenEvolve 进化代码 vs 人工修复的采纳率对比 | 生产环境分流 |
| **进化代码功能测试** | OpenEvolve 产出的代码是否通过原始功能测试 | CI 自动跑 |
| **门控回归测试** | TBLite / YC-Bench 等基准，确保进化代码不会导致全局退化 | 周循环自动触发 |

### 特定测试

- **Static Gate 规则测试**: 构造高复杂度代码、含 SQL 注入的代码、硬编码密钥的代码，验证是否被正确阻断
- **Agent 冲突测试**: 构造"加安全校验会导致函数超长"的场景，验证 Arbiter 是否按优先级正确仲裁
- **进化回滚测试**: 验证当 OpenEvolve 产出的代码采纳率持续下降时，系统能否自动降低进化优化权重，回归到人工修复建议模式

---

## Out of Scope

以下内容明确**不在 v1.0 范围内**：

1. **自动合并 PR** — v1.0 中所有 PR 仍需至少 1 名人工审查员 approve，AI 不自动合并代码
2. **自动生成修复代码并提交** — 只提供修复建议和补丁，由开发者决定是否应用
3. **实时编码辅助** — 系统在 PR 阶段触发，不做 IDE 内实时审查（那是 IDE 插件的范畴）
4. **跨仓库横向对比** — 仅对本仓库的代码做纵向趋势分析，不和外部仓库做 benchmark 对比
5. **测试用例生成** — 系统不会根据代码变更自动生成测试用例（那是另一个产品需求）
6. **架构设计审查** — Agent 不评估架构合理性、不推荐设计模式重构（这些属于人工审查员的判断领域）
7. **自然语言需求文档审查** — 只审查代码，不审查 PR Description 或需求文档质量

---

## Further Notes

### 风险与缓解

| 风险 | 缓解措施 |
|------|---------|
| 审查噪音过大，开发者关闭 AI 审查 | Phase 2 先只展示 🔴 阻断项；提供"一键关闭"开关 |
| OpenEvolve 进化导致代码功能退化 | 功能正确性测试作为硬门控 + 进化后的代码需人工 review diff |
| 高复杂度阻断引发团队抵触 | "先警告后阻断"策略 + OpenEvolve 自动提供重构方案降低抵触 |
| LLM API 成本超出预期 | 使用 Gemini Flash 做大部分审查 + 缓存相似 diff 结果 + OpenEvolve 进化只对 top-N 问题触发 |
| 敏感代码泄露给外部 LLM | Phase 2 后端必须支持本地 Ollama 模式；OpenEvolve 也使用本地模型 |


### 成功指标（KPI）

**系统健康度（周报）**:
- AI 建议采纳率 > 60%
- 阻断准确率 > 80%
- 漏报率（未被检测的线上 bug / 总线上 bug）< 10%
- 审查总耗时（PR 创建 → 所有检查完成）< 10 min

**代码质量趋势（月报）**:
- 平均圈复杂度持续下降
- 高复杂度函数占比持续下降
- 每 PR 阻断项平均数持续下降

**进化代码质量（Phase 2+）**:
- OpenEvolve 进化代码的功能测试通过率 = 100%（硬门控）
- OpenEvolve 进化代码的采纳率 > 50%（开发者选择使用进化版本而非自己手写修复）
- 进化代码的平均复杂度降低幅度 > 30%
- MAP-Elites 网格填充率随时间增加（发现更多"复杂度-性能"组合的解）

### 四阶段实施计划

| 阶段 | 时间 | 目标 | 关键交付 |
|------|------|------|---------|
| **Phase 1: 止血** | 第 1-2 周 | 阻断最糟糕代码进入主分支 | CI 静态门控 + 复杂度/安全硬阻断 |
| **Phase 2: 增强** | 第 3-4 周 | AI 辅助审查 + 代码自动进化 | 5 Agent + Arbiter + **OpenEvolve 代码进化引擎** + 审查报告 |
| **Phase 3: 优化** | 第 5-8 周 | 进化质量提升 + 反馈闭环 | Prompt 调优 + 反馈数据驱动的评估器权重调整 + 度量看板 |
| **Phase 4: 自驱** | 第 9-12 周 | 持续优化循环 | 阈值自适应 + CI/CD 全集成 + 成本/延迟监控 |

### 参考

- 架构设计文档: [代码审查智能体系统_架构设计与功能方案](./代码审查智能体系统_架构设计与功能方案.md)
- OpenEvolve: [GitHub](https://github.com/algorithmicsuperintelligence/openevolve)
- Hermes Agent Self-Evolution (GEPA 参考): [GitHub](https://github.com/NousResearch/hermes-agent-self-evolution)
